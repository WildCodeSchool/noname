{% macro ratingStars(rating) %}
	<div class="ratingStars">
		{% for i in range(1, 5) %}
			{% if rating < i %}
				<img src="/assets/svg/star.svg"/>
			{% else %}
				<img src="/assets/svg/starChecked.svg"/>
			{% endif %}
		{% endfor %}
	</div>
{% endmacro %}
